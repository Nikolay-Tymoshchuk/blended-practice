import { AddContactForm } from 'components/AddContactForm';

export const AddContactView = () => {
  return (
    <section>
      <h2> Add Contact</h2>
      <AddContactForm />
    </section>
  );
};
